export const INDENTATION = 40
export const MAX_DEPTH = 3

export const SORTABLE_CONFIG = {
    alignment: {
        x: 'start',
        y: 'center',
    },
    transition: {
        idle: true,
    },
} as const
